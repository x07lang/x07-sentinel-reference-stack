{
  "schema_version": "lp.release.submit.request@0.1.0",
  "workload_id": "${RECONCILIATION_JOB_WORKLOAD_ID}",
  "environment_id": "${ENVIRONMENT_ID}",
  "target_id": "${TARGET_ID}",
  "pack_digest": "sha256:${RECONCILIATION_JOB_PACK_DIGEST}",
  "rollout_strategy": "manual",
  "notes": "reference stack reconciliation-job"
}
