# order-domain

Shared contract artifacts for the Sentinel reference stack.

This directory is intentionally lightweight and stays **pure** (no network, no filesystem).

It provides:
- JSON schema contracts under `contracts/`
- examples under `examples/`
- a shared x07 package (`x07-package.json`) with helpers under `modules/`
